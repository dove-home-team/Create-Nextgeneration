package io.github.dovehome.create.next.generation.forge;

import io.github.dovehome.create.next.generation.CreateNextGeneration;
import net.minecraftforge.fml.common.Mod;

@Mod(CreateNextGenerationForge.MODID)
public class CreateNextGenerationForge extends CreateNextGeneration {
    public CreateNextGenerationForge() {
        init();
    }
}
